package ProcessPetitions;

public class CheckExceptions {
    //We're only going to check for exceptions for a given month, since that's the only part that's relevant
    //There could be a method that removes the petitions that cannot be processed

}